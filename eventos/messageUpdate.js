const Discord = require("discord.js");

module.exports = async (bot, message, oldMessage, newMessage) => {
  /*
  let logCH = await bot.channels.cache.get("744017110518464612");
  if (oldMessage.author.bot || newMessage.author.bot) return;
  let teste = new Discord.MessageEmbed()
    .setColor("RED")
    .setTitle("Mensagem editada!")
    .setDescription(
      "O usuário <@" +
        oldMessage.author.id +
        "> modificou uma mensagem\n Antiga mensagem\n`" +
        oldMessage.content +
        "`\nNova Mensagem\n`" +
        newMessage.content +
        "`"
    );

  bot.channels.cache.get(logCH).send(teste);
  */
};
